php-version: 8.4
extensions: "bcmath,brotli,bz2,calendar,ctype,curl,exif,fileinfo,filter,gd,iconv,intl,libxml,mbregex,mbstring,mysqli,mysqlnd,openssl,pdo,pdo_sqlite,phar,simplexml,sqlite3,tokenizer,xml,xmlwriter,zip,zlib"
sapi:
  - cli
# download-options:
#   retry: 5
#   ignore-cache-sources: php-src
#   prefer-pre-built: true
build-options:
  with-upx-pack: true
